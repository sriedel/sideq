## 0.1.3
- Add "retry list csv" and "dead list csv" commands for better parsing in scripts
- Bug: Fix type in help text: "queues list" instead of "queue list"

## 0.1.2 
- Bug: Fix job accessor name error
- Bug: Fix reported job number when clearing retry set and dead set

## 0.1.1
- Add per-queue information and manipulation 
- Rename 'queue list' command to 'queues list' to make place for the per-queue commands
- Add next execution time to details of the retry set

## 0.1.0
- Initial release
